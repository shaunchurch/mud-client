{
  "name": "mud-client",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/raw/index.ts",
    "dev": "bun --watch run src/raw/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "build": "bun build --compile --outfile=mud-client src/raw/index.ts",
    "release": "gh release create --generate-notes"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
