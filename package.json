{
  "name": "mud-client",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/raw/index.ts",
    "dev": "bun --watch run src/raw/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
